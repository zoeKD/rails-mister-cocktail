<%= simple_form_for [cocktail, review] do |f| %>
  <%= f.input :content, label:false, placeholder: "Loving it so much!" %>
  <%= f.input :rating, collection: 1..5, as: :radio_buttons %>
  <%= f.submit class: "btn primary-button-full button-large" %>
<% end %>


